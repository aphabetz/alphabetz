<!-- <% if (popupbanners.length > 0) { %>
    <div id="popback" onclick="closepopup()"></div>
    <div id="popup-banner">
        
      <button id="close-popup" class="hover1">&times;Close</button>
       
        <div class="desktop-popup-banner">
            <div class="swiper-container desktop-banner" id="desktop-popup-banners">
                <div class="swiper-wrapper">
                    <% popupbanners.forEach(slide => { %>
                        <div class="swiper-slide bgimg">
                            <img src="<%= process.env.S3_BASE_URL + slide.bannerImage.webView %>" alt="<%= slide.title %>"/>
                           
                        </div>
                         
                    <% }) %>
                </div>
            </div>
        </div>
  
        <div class="mobile-popup-banner">
            <div class="swiper-container mobile-banner" id="mobile-popup-banners">
                <div class="swiper-wrapper">
                    <% popupbanners.forEach(slide => { %>
                        <div class="swiper-slide bgimg">
                            <img src="<%= process.env.S3_BASE_URL + slide.bannerImage.mobileView %>" alt="<%= slide.title %>"/>
                        </div>
                    <% }) %>
                </div>
            </div>
        </div>
    </div>
  <% } %>

 

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const bannersExist = document.querySelectorAll('#popup-banner .swiper-slide').length > 0;

    if (!bannersExist) {
        return;
    }

    const popupShown = sessionStorage.getItem('popbackShown');
    if (popupShown) {
        return;
    }

    // Show the popup
    const popupBanner = document.getElementById('popup-banner');
    const popback = document.getElementById('popback');

    popupBanner.style.display = 'flex';
    popback.style.display = 'flex';
    document.body.style.overflow = 'hidden';

    sessionStorage.setItem('popbackShown', 'true');

    // Delay Swiper initialization
    setTimeout(() => {
        initializeSwipers();
    }, 300);

    document.getElementById('close-popup').addEventListener('click', closepopup);
    popback.addEventListener('click', closepopup);
});

function initializeSwipers() {
    // Destroy existing Swipers
    if (window.desktopSwiper) window.desktopSwiper.destroy(true, true);
    if (window.mobileSwiper) window.mobileSwiper.destroy(true, true);

    // Initialize Swiper for Desktop
    window.desktopSwiper = new Swiper('#desktop-popup-banners', {
        slidesPerView: "1",
        centeredSlides: true,
        spaceBetween: 30,
        loop: true,
        autoplay: {
            delay: 3000,
            disableOnInteraction: false, // Keeps autoplay running even after interaction
        },
        pagination: { el: '.swiper-pagination', clickable: true },
        navigation: { nextEl: '.swiper-button-next', prevEl: '.swiper-button-prev' },
    });

    // Initialize Swiper for Mobile
    window.mobileSwiper = new Swiper('#mobile-popup-banners', {
        slidesPerView: "1",
        centeredSlides: true,
        spaceBetween: 30,
        loop: true,
        autoplay: {
            delay: 3000,
            disableOnInteraction: false,
        },
        pagination: { el: '.swiper-pagination', clickable: true },
        navigation: { nextEl: '.swiper-button-next', prevEl: '.swiper-button-prev' },
    });

    // Update Swiper to ensure proper rendering
    setTimeout(() => {
        window.desktopSwiper.update();
        window.mobileSwiper.update();
    }, 500);
}

function closepopup() {
    const popupBanner = document.getElementById('popup-banner');
    const popback = document.getElementById('popback');

    popupBanner.style.display = 'none';
    popback.style.display = 'none';
    document.body.style.overflow = 'visible';

    sessionStorage.setItem('popbackShown', 'true');
}

</script> -->
<!--    
      
    <% banners.forEach(slide => { %>
         -->
          <!-- <% }) %> --> 
        <section class="Hero">
            <div class="container">
              <div class="d-flex justify-content-center align-items-center text-center flex-column" data-aos="fade-up">
                <h1 style="font-weight:700 !important;">
                  Welcome to Alphabetz Preschool Dongargarh
                  </h1>
                <p>Nurturing creativity, life skills, and playful learning in a supportive environment, guided by dedicated teachers for your child’s growth and development.
                </p>
                <div>
                  <a href="/admisson" class=" hovering">Get In Touch</a>
                </div>
              </div>
              <div
                class="d-flex align-items-center justify-content-center gap-lg-5 pt-lg-5 position-relative flex-md-row flex-column">
                <figure>
                  <img class="position-absolute rotate-animation" src="assets/images/index/plane.webp" alt="plane">
                </figure>
                <div>
                  <div class="p-0"></div>
                  <figure>
                    <img class="position-absolute bounce-animation w-10" src="/assets/images/alphabetz-preschoolmascot-main1.webp" alt="pencils">
                  </figure>
                </div>
                <div class="d-flex flex-column gap-0">
                  <div class="d-flex flex-column align-items-center gap-2">
                    <h2><span class="number" data-final-value="7.5"></span>Learn
                      
                      </h2>
                    <p>Smart Methods, Easy Learning</p>
                  </div>
                  <hr>
                  <div class="d-flex flex-column align-items-center gap-2">
                    <h2><span class="number" data-final-value="50"></span>Fun</h2>
                    <p>Where play and education <br> go hand in hand.</p>
                  </div>
                </div>
                <div
                  class="d-flex align-items-md-end justify-content-center gap-lg-3 gap-md-2 gap-3 position-relative flex-md-row flex-column mt-md-0 mt-4">
                  <figure class="m-0" data-aos="zoom-in-up">
                    <img class=" img-1" style="width:63%; margin-left:80px" src="/assets/images/commits/AlphabetzPreschoolMascotaward.webp" alt="HeroImg">
                  </figure>
                  <div class="position-relative" data-aos="zoom-in-up">
                    <figure>
                      <img class="img-2 rounded-circle w-50" style="width:50%;  margin-left:110px" src="assets/images/index/pencils.webp" alt="HeroImg2">
                    </figure>
                  </div>
                  <div>
                    <div class="p-0 z-1"></div>
                    <figure>
                      <img class="position-absolute bounce-slow" src="assets/images/index/bulb.webp" alt="bulb">
                    </figure>
                  </div>
                  <figure>
                    <img class="position-absolute rotate-animation" src="assets/images/index/baloon.webp" alt="Baloon">
                  </figure>
                </div>
              </div>
              
            </div>
        </section>

   
    

